import { NextToJumpResponse } from './next-to-jump-response';

describe('NextToJumpResponse', () => {
  it('should create an instance', () => {
    expect(new NextToJumpResponse()).toBeTruthy();
  });
});
